agent:
  server_url: "http://localhost:8080"
  websocket_path: "/api/v1/agents/ws"
  force_websocket_from_config: false
  # 在控制台「添加节点」后填入以下三项（与数据库中一致）；无需再调用注册接口。
  agent_id: ""
  agent_secret: ""
  node_key: ""
  # node_id: 123   # 可选，与控制台节点 ID 一致时便于日志
  node_name: "example-node"
  data_dir: "./data"
  config_dir: "./data/config"
  credential_dir: "./data/credentials"
  log_dir: "./data/logs"
  runtime_interval_seconds: 30
  heartbeat_interval_seconds: 15
  reconnect_interval_seconds: 5
  request_timeout_seconds: 10
  agent_version: "0.1.0"
  disable_auto_update: false
  self_update_period_minutes: 0
  github_repo: "nexctl/agent"
